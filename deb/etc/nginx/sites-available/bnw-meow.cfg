server {
    listen 80;
    server_name meow.bnw.im;
    root /srv/bnw-meow/;

    location / {
        try_files $uri /index.html;
    }

    location ~ ^/favicon.png$|^/static/ {
        expires 1y;
        add_header Cache-Control public;
    }
}
