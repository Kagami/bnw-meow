server {
    listen 80;
    server_name meow.genshiken.org;

    location ~ ^/index.html$|^/favicon.ico$|^/static/ {
        root /srv/bnw-meow/;
    }

    location / {
        rewrite .* /index.html last;
    }
}
